[project]
name = "solana-inspector"
version = "0.1.0"
description = "MCP server for Solana blockchain inspection"
dependencies = [
    "mcp[cli]>=1.0.0",
    "solana>=0.30.0",
    "solders>=0.18.0",
    "httpx>=0.25.0"
]
requires-python = ">=3.10"

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"