[project]
name = "crypto-quant"
version = "0.1.0"
description = "MCP server for Quantitative Analysis and Backtesting"
dependencies = [
    "mcp[cli]>=1.0.0",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "pandas_ta>=0.3.14b",
    "yfinance>=0.2.0"
]
requires-python = ">=3.10"

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"